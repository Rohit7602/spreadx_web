import 'package:flutter/material.dart';

class InventoryAddSuppplier extends StatelessWidget {
  const InventoryAddSuppplier({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
